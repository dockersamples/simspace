# Demo Project

This is the starter project for the **Sandbox Lifecycle** lab. It's a plain
workspace you can explore with normal shell commands (`ls`, `cat`, an editor)
while you follow the instructions.

Nothing here is special — it exists so the workspace isn't empty when your
sandbox starts.
